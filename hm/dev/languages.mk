
# These are custom installs handled in the Makefile directly
# - Go: download from golang.org
# - Rust: via rustup
# - Node: via nvm or apt
# - Python: via apt + uv
# - Flutter/Dart: via flutter SDK

APT += python3 python3-pip nodejs npm gcc make
